<!DOCTYPE html>
<html>
<head>
    <title>MGG - Home</title>
    <link href="https://stackpath.bootstrapcdn.com/bootstrap/4.5.2/css/bootstrap.min.css" rel="stylesheet">
</head>
<body>
    <div class="container mt-5">
        <h1>Welcome to MGG</h1>
        <p>Your platform for modern and customizable bio links.</p>
        <% if (!user) { %>
            <a href="/login" class="btn btn-primary">Login with Discord</a>
        <% } else { %>
            <a href="/profile/<%= user.custom_username || user.id %>" class="btn btn-primary">Go to Profile</a>
        <% } %>
    </div>
</body>
</html>
