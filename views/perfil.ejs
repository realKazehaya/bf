<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Profile</title>
</head>
<body>
    <h1>Your Profile</h1>
    <form action="/perfil/update" method="post">
        <label for="description">Description:</label>
        <textarea id="description" name="description"><%= profileInfo.description || '' %></textarea>
        <br>
        <label for="socialLinks">Social Links (comma separated):</label>
        <input type="text" id="socialLinks" name="socialLinks" value="<%= profileInfo.socialLinks || '' %>">
        <br>
        <button type="submit">Update Profile</button>
    </form>
    <a href="/">Go Home</a>
    <a href="/logout">Logout</a>
</body>
</html>
