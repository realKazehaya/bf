<!DOCTYPE html>
<html>
<head>
    <title><%= user.custom_username || user.username %>'s Profile</title>
    <link href="https://stackpath.bootstrapcdn.com/bootstrap/4.5.2/css/bootstrap.min.css" rel="stylesheet">
</head>
<body>
    <div class="container mt-5">
        <h1><%= user.custom_username || user.username %>'s Profile</h1>
        <p><strong>Email:</strong> <%= user.email %></p>
        <p><strong>Description:</strong> <%= user.description || 'No description' %></p>
        <p><strong>Social Links:</strong> <%= user.social_links || 'No links' %></p>
        <p><strong>Views:</strong> <%= user.views %></p>
        <a href="/settings" class="btn btn-primary">Edit Profile</a>
    </div>
</body>
</html>
