<!DOCTYPE html>
<html lang="es">
<head>
  <meta charset="UTF-8">
  <title>Perfil</title>
</head>
<body>
  <h1>Perfil de <%= user.freefire_id %></h1>
  <p>Diamantes: <%= user.diamonds %></p>
  
  <h2>Historial de retiros</h2>
  <ul>
    <% withdraws.forEach(withdraw => { %>
      <li><%= withdraw.amount %> diamantes - <%= withdraw.status %> - Fecha: <%= withdraw.date %></li>
    <% }) %>
  </ul>

  <a href="/logout">Cerrar Sesión</a>
</body>
</html>
