<!DOCTYPE html>
<html>
<head>
    <title>Settings</title>
</head>
<body>
    <h1>Settings</h1>
    <form action="/settings" method="post">
        <h2>Account Overview</h2>
        <p><strong>Username:</strong> <%= user.username %></p>
        <p><strong>ID:</strong> <%= user.id %></p>
        <p><strong>Profile Views:</strong> <%= user.views %></p>

        <h2>Account Settings</h2>
        <label for="description">Description:</label>
        <textarea id="description" name="description"><%= user.description %></textarea>

        <label for="socialLinks">Social Links (JSON format):</label>
        <textarea id="socialLinks" name="socialLinks"><%= JSON.stringify(user.social_links || {}) %></textarea>

        <label for="discordId">Discord ID:</label>
        <input type="text" id="discordId" name="discordId" value="<%= user.discord_id || '' %>">

        <button type="submit">Save Changes</button>
    </form>
    <a href="/upload">Upload Assets</a>
</body>
</html>
